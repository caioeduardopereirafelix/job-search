{
  "name": "job-search-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend estatico (TS -> JS, sem framework) servido pelo Spring Boot em src/main/resources/static",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
